<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Singlebasket">
	<select id="mBasketlist" resultType="msdto" parameterType="int">
		select n.idx,f.fname,f.price,n.count,n.today from food f 
		Inner JOIN nm_basket n on f.idx=n.fidx where n.midx=#{midx};
	</select>
</mapper>